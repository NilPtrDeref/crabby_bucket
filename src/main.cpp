#include "engine.h"

// TODO: Convert to raylib. Final pass needed, game runs. Need to double-check collision logic (and
// other things).
// TODO: Rotation for crab based on speed?
// TODO: Animations on claws?
int main() {
  { Engine engine; }
  return 0;
}
